@model IEnumerable<ViewModels.BukuVM>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            ID Buku
        </th>
        <th>
            Judul Buku
        </th>
        <th>
            Pengarang
        </th>
        <th>
            Jenis Buku
        </th>
        <th>
            Harga Sewa
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ID_Buku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JudulBuku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pengarang)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.jenisBuku)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HargaSewa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Update", new { id=item.ID_Buku }) |            
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Buku })
        </td>
    </tr>
}

</table>
